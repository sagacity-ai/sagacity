# Docs toolchain, pinned so CI and local builds agree.
# Rebuild locally with:
#   python3 -m venv .venv && .venv/bin/pip install -r docs/requirements.txt
#   .venv/bin/mkdocs serve
mkdocs-material==9.7.7
