<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.sumitvairagar</groupId>
        <artifactId>sagacity-parent</artifactId>
        <version>0.1.0</version>
    </parent>

    <artifactId>sagacity-coverage</artifactId>
    <name>Sagacity Coverage Report</name>
    <description>Aggregate JaCoCo report across all modules. Not published.</description>

    <!--
      Per-module JaCoCo reports understate coverage: a class in sagacity-core
      exercised only by sagacity-spring-ai tests shows as 0% in core's own
      report. This module depends on every published module so report-aggregate
      can merge the execution data into one honest number.

      Build last (it is the final <module> in the parent) and read:
        sagacity-coverage/target/site/jacoco-aggregate/index.html
    -->

    <properties>
        <maven.deploy.skip>true</maven.deploy.skip>
        <maven.install.skip>true</maven.install.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.github.sumitvairagar</groupId>
            <artifactId>sagacity-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.sumitvairagar</groupId>
            <artifactId>sagacity-spring-ai</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.sumitvairagar</groupId>
            <artifactId>sagacity-spring-boot-starter</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>aggregate-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report-aggregate</goal>
                        </goals>
                        <configuration>
                            <title>Sagacity — aggregate coverage</title>
                            <outputDirectory>${project.build.directory}/site/jacoco-aggregate</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
